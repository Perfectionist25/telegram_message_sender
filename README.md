🧾 Что делает этот проект

Этот проект — скрипт на Python, который отправляет сообщения через Telegram с использованием библиотеки Telethon и учётных данных твоего Telegram-аккаунта.

✅ 1. Установка Python

Если Python ещё не установлен:

Скачай и установи Python 3.10+ с официального сайта:
https://www.python.org/

Убедись, что поставил галочку Add to PATH при установке.

Проверь в терминале:

python --version
pip --version

🗂️ 2. Скачивание репозитория

Открой терминал (CMD / PowerShell / Terminal) и выполни:

git clone https://github.com/Perfectionist25/telegram_message_sender.git
cd telegram_message_sender


Это скачает проект на твой компьютер.

📦 3. Установка зависимостей

В корне проекта должен быть файл requirements.txt — это список нужных пакетов.

Установи зависимости:

pip install -r requirements.txt


Если файл есть — команды выполнятся. Если файла нет, то, скорее всего, используется только Telethon:

pip install telethon

🛠️ 4. Настройка API доступа

Чтобы Telethon работал, тебе нужны API ID и API Hash от Telegram.

Зайди на https://my.telegram.org

Войди под своим номером телефона

Перейди в API Development Tools

Скопируй:

api_id

api_hash

В коде (в например message_sender.py) пропиши:

api_id = ТВОЙ_API_ID
api_hash = 'ТВОЙ_API_HASH'
my_phone = '+ТВОЙ_НОМЕР'

💬 5. Первый запуск

Проект содержит скрипт, например:

python message_sender.py


или

python text-message.py


Если такого файла нет — открой их в редакторе (VSCode / PyCharm) и посмотри, как запускается основная функция — обычно там есть блок:

if __name__ == "__main__":
    asyncio.run(main())


Тогда команда:

python message_sender.py


откроет сессию Telegram и начнёт отправку сообщений.

💾 6. Первая авторизация

При первом запуске:

Telethon создаст файл сессии (например session.session)

Телеграм пришлёт код в Telegram-приложение

Введи код в консоли

Этот файл нужна, чтобы больше не вводить код при следующем запуске.

📣 7. Отправка сообщений

Скрипт должен содержать переменную с целевыми номерами и текстом, например:

target_phone = "+998xxxxxxxxx"
message = "Привет! Это тест."


Скрипт отправит сообщение указанному номеру.

🧪 8. Советы по использованию

🕐 Не спамь сильно — Телеграм может ограничить запросы

🔑 Никогда не выкладывай свой api_id и api_hash публично

📌 Храни файл сессии в безопасном месте

🧩 Примеры команд

Запустить с первым скриптом:

python message_sender.py


Если есть второй скрипт:

python text-message.py

📝 Проблемы и их решения
Проблема	Решение
Ошибка «No module named Telethon»	Установи pip install telethon
Скрипт не запускается	Убедись, что ты в директории проекта
Telegram возвращает FloodWaitError	Добавь задержки между отправками
